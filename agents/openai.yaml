interface:
  display_name: "Vox Agent"
  short_description: "用 GPT Image 2/Liblib 回退、Kling 与 Fish 制作 VOX 视频"
  default_prompt: "Use $vox-agent to turn this topic into a finished Chinese VOX-style collage explainer video."
